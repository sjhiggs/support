apiVersion: v1
kind: ConfigMap
metadata:
  name: bootstrap-post-config
data:
  post-config.sh: |
    #!/bin/bash
    echo "Copying custom bootstrap.xml to ${CONFIG_INSTANCE_DIR}/etc/"
    cp /amq/custom/bootstrap.xml ${CONFIG_INSTANCE_DIR}/etc/bootstrap.xml
